package magic

/**
 * Represents white magic spells in the game.
 *
 * The `WhiteMagic` trait extends the `Magic` trait and defines the common interface for all white magic spells.
 * White magic spells are typically defensive or supportive in nature and can be used by characters that specialize in white magic.
 */
trait WhiteMagic extends Magic
